
type superprogram {

type program2( ){ 

    x(r) =>{
        let y = 30;
    };

    m()=>{

    };

};

type program(  ) inherits program2() {
    x = 20;

    tan( x ) => {
        
        let z = new program2( );
        z.x(x);
        let a = false;
    };
};
};
