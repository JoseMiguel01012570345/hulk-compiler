
type superprogram {

    type program2(  ){

        v(x,m)=>{

        };

    };

    type program( a , b ) inherits program2( a , b ) {
        
                function XXX( ) {
                    
                    let v = new program2();
                    v.v(v);
                    
                };
        };
};
