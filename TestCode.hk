type superprogram{

type program2( a ,c ){

};

type program( a ,b ) inherits program2( a ,b  ) {

      let x = 10;

     function tan(x) { 

        let c = new program( a ,b );        
        sin(c) /  cos(c);
     };
};

};
